export enum ErrorConstants {
    USER_NOT_FOUND = 'user not found',
    VEHICLE_NOT_FOUND = 'vehicle not found',
    LIKE_HAS_ALREADY_BEEN_COMPUTED = 'this like has already been computed',
    LIKE_LIMIT_REACHED = 'like limit has been reached',
}
